# 已完成的事
* 安排了寒假的工作

# 待完成事项
* 使用docker,mcp,LangGraph构建
* 细化架构

``` mermaid
    graph TD
    %% 用户交互层
    User[用户 (A)] -- "1.自然语言需求" --> Parser[需求解析 Agent (B)]
    
    %% 核心状态管理层 (LangGraph State)
    Parser -- "2.生成/更新 Spec (JSON)" --> StateDB[(全局状态 State)]
    StateDB -- "3.反馈/确认" --> User
    
    %% 决策与路由层
    StateDB --> Manager[任务路由/架构师 (C)]
    
    %% IP 复用层 (新增)
    Manager -.-> IPLib[IP 资产库 (MCP)]
    IPLib -.-> Manager
    
    %% 并行生成层 (L3 Agents)
    Manager -- "4a.分发 Spec" --> GenAgent[HDL 生成 Agent (D)]
    Manager -- "4b.分发 Spec" --> TestAgent[验证生成 Agent (E1)]
    
    %% 沙盒执行层 (Docker)
    GenAgent -- "5. Verilog 代码" --> Sandbox[Docker 执行环境 (E2)]
    TestAgent -- "5. Python/pyuvm 代码" --> Sandbox
    
    %% 核心反馈闭环
    Sandbox -- "6. 运行仿真" --> Runner(Verilator/Cocotb)
    Runner -- "7. 结构化日志 (JSON Log)" --> Analyzer[日志分析/判题器]
    
    %% 路由闭环
    Analyzer -- "8a. Pass" --> StateDB
    Analyzer -- "8b. Fail (带归因)" --> Manager
```