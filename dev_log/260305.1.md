# 2026-03-05 优化精简记录（本轮）

## 目标
- 在不改变核心架构边界（Core App / Sandbox）的前提下，继续推进代码与文档的整理、精简与一致性优化。
- 统一开发测试脚本入口与导入方式，减少重复逻辑。
- 清理历史拼写兼容层与无效依赖，降低维护负担。

## 用户决策记录（强约束）
- **保留 `scripts/dev_tests/`，不对此目录做任何更改。**
- 本轮仅记录该决策，不删除、不迁移、不覆盖该目录内容。

## 本轮已完成优化

### 1) 预处理模块命名与导入一致化
- 删除历史拼写文件：`app/pre_agent/stracture_request.py`
- 统一使用规范模块：`app/pre_agent/structure_request.py`
- 更新导入引用：
  - `app/pre_agent/__init__.py`
  - `app/pre_agent/parser_agent.py`

### 2) 开发测试脚本去重（app 内可运行路径）
- 新增公共引导：`app/dev_tests/_bootstrap.py`
- 统一 `app/dev_tests` 脚本使用公共引导，避免重复路径注入代码：
  - `run_pre_manager_unit_basic.py`
  - `run_pre_manager_workflow.py`
  - `run_tui.py`
  - `run_tui_components.py`
  - `run_config_workflow.py`
  - `run_encryption.py`
  - `run_test_agent_docker_integration.py`
- 更新目录说明：`app/dev_tests/README.md`

### 3) 依赖与实现语义对齐
- 从 `requirements.txt` 移除未使用依赖：`pqcrypto`
- 保留并明确当前实现为 `RSA-OAEP + AES-GCM`（与实际代码一致）

### 4) 文档一致性修复
- 已同步修复旧路径/旧命名/旧日期引用，涉及：
  - `README.md`
  - `DEVELOPMENT_SUMMARY.md`
  - `docs/spec/agent-config.md`
  - `docs/testing/docker-unit-tests.md`
  - `docs/testing/TEST_RESULTS_2026-02-14.md`
  - `docs/testing/TEST_RESULTS_2026-03-04.md`
  - `docs/architecture/JINJA2_IMPLEMENTATION_GUIDE.md`

## Docker 回归验证结果

### 执行命令
1. `docker compose exec -T agent-core python3 app/dev_tests/run_pre_manager_unit_basic.py`
2. `docker compose exec -T agent-core python3 app/dev_tests/run_encryption.py`
3. `docker compose exec -T agent-core python3 app/dev_tests/run_tui.py`
4. `timeout 300s docker compose exec -T agent-core python3 app/dev_tests/run_test_agent_docker_integration.py`

### 结果摘要
- Pre-Manager 脚本测试：**9/9 通过**
- 加解密脚本：**通过**
- TUI 脚本：**通过**
- TestAgent + Sandbox 集成：**通过**（`demo_top` / `demo_leaf` 均 pass）

## 风险与说明
- 编辑器本地环境可能出现第三方包导入提示（如 `langchain_*`），以 Docker 运行结果为准。
- 本轮不触碰 `scripts/dev_tests/`，后续若需去重，应先由用户确认策略后再执行。

## 结论
- 本轮优化已完成“结构一致化 + 冗余降低 + 文档同步 + Docker 验证闭环”。
- 严格遵循用户约束，已保留 `scripts/dev_tests/` 原样不变。
